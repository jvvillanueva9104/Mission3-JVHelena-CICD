{
  "compilerOptions": {
    "types": ["node"],
    "target": "ESNext",
    "module": "CommonJS",
    "outDir": "functions apis", // Output directory for compiled files
    "rootDir": "./", // Directory where your test files are located
    "strict": true,
    "esModuleInterop": true
  },
  "include": [
    "functions/src/**/*.ts",
    "functions/test/**/*.ts",
    "apis/src/**/*.ts",
    "apis/test/**/*.ts" // Adjust the pattern based on your test file location
  ],
  "exclude": [
     // Exclude your main application source files
  ]
}
